version: 2

goal:
  title: "NavMap Workflow Canvas Embed Mode"
  slug: "nav-map-workflow-canvas-embed-mode"
  kind: existing_plan
  tranche: "Freeze the shared canvas contract, then build and prove a focused reusable WorkflowCanvas without Agent Workflows lifecycle inference."
  status: active
  oracle:
    signal: "A standalone focused WorkflowCanvas renders the frozen workflow-canvas/v1 fixture accessibly at desktop and 393x852 with truthful screenshot nodes, supplied run overlays, findings/annotations, focused details, and comparisons while unrelated NavMap controls remain absent."
    cadence: "after each Worker package and at final audit"
    final_proof: "Receipt-backed component, semantic fallback, accessibility, responsive, reduced-motion, fixture, package-consumer, full validation, and independent final Judge proof mapped to all charter checks."
  intake:
    original_request: "Perform a focused NavMap workflow-canvas track in parallel with the Studio decision-model and journey-evidence track."
    interpreted_outcome: "NavMap offers a constrained reusable workflow-review canvas that consumes but never invents Agent Workflows truth."
    input_shape: existing_plan
    audience: "Product teams embedding screenshot-backed workflow review, initially Agent Workflows Studio"
    authority: approved
    proof_type: demo
    completion_proof: "The public focused component passes fixture, interaction, responsive, accessibility, reduced-motion, package-consumer, full validation, and final adversarial proof."
    likely_misfire: "Repackage the complete NavMap viewer, hide complexity with brittle CSS, or hardcode Agent Workflows lifecycle semantics."
    blind_spots_considered:
      - "React and layout runtime weight"
      - "Public exports versus private internal composition"
      - "Producer-consumer contract drift"
      - "Generic presentation versus product-specific lifecycle inference"
      - "Reference, observed, resolution, unavailable, stale, and corrupt evidence"
      - "Annotation provenance and immutable screenshot bytes"
      - "Mobile canvas gestures, panel density, and page overflow"
      - "Keyboard graph traversal, focus return, dialogs, and screen-reader fallback"
      - "Reduced motion and decorative animation"
      - "Larger graph performance and semantic zoom"
      - "Privacy-safe fixtures and package delivery"
    existing_plan_facts:
      - "Run consumer and producer Scouts in parallel before contract freeze."
      - "Workflows owns lifecycle meaning and the canonical producer contract after rendezvous."
      - "NavMap renders supplied truth and never derives pass, qualification, integrity, readiness, or canonical eligibility."
      - "Expose a focused public WorkflowCanvas API rather than private component imports or the full viewer."
      - "Default to Flow view and hide unrelated search, live-target, analytics, export, route-health, and site-map controls."
      - "Support screenshot nodes, focused details, supplied status/findings/annotations, semantic fallback, and comparison."
      - "Prove desktop, exactly 393x852 mobile, keyboard, screen readers, reduced motion, and package consumption."
      - "Stop before production Agent Workflows integration or package publication."

rules:
  pm_owns_state: true
  one_active_task: true
  max_write_workers: 1
  no_implementation_without_worker_or_pm_task: true
  no_completion_without_judge_or_pm_audit: true
  planning_is_not_completion: true
  queued_required_worker_blocks_completion: true
  continuous_until_full_outcome: true
  missing_input_or_credentials_do_not_stop_goal: true
  exact_human_approval_can_terminal_wait: true
  preserve_and_validate_existing_plan: true
  intake_misfire_must_be_audited: true
  goal_pressure_requires_oracle: true
  no_completion_on_weak_proof: true
  slice_policy:
    max_consecutive_tiny_tasks: 2
    prefer_vertical_slices: true
    judge_picks_largest_safe_slice: true
    worker_completes_whole_slice: true

agents:
  scout: installed
  worker: installed
  judge: installed

visual_board:
  selected: local
  local:
    status: live
    url: "http://goalbuddy.localhost:41740/nav-map-workflow-canvas-embed-mode/"
    command: "node /Users/neonwatty/.codex/plugins/cache/goalbuddy/goalbuddy/0.4.3/skills/goal-prep/surfaces/local-goal-board/scripts/local-goal-board.mjs --goal docs/goals/nav-map-workflow-canvas-embed-mode --port 41740"

active_task: T007

tasks:
  - id: T001
    type: scout
    assignee: Scout
    status: done
    reasoning_hint: high
    objective: "Audit the smallest credible focused workflow-canvas consumer boundary."
    receipt:
      result: done
      summary: "Mapped the reusable graph, semantic, responsive, accessibility, and package seams and rejected composition through the complete NavMap viewer."
      evidence:
        - packages/core/src/index.ts
        - packages/core/src/components/NavMap.tsx
        - packages/core/src/workflow-canvas/types.ts
      harness: codex

  - id: T002
    type: judge
    assignee: Judge
    status: done
    reasoning_hint: high
    objective: "Freeze and verify the producer-consumer workflow-canvas/v1 contract."
    receipt:
      result: done
      decision: approved
      full_outcome_complete: false
      summary: "Accepted Workflows authority for lifecycle truth and froze the direct public WorkflowCanvas boundary."
      evidence:
        - docs/goals/nav-map-workflow-canvas-embed-mode/notes/T002-supplied-shared-contract.md
      commands:
        - cmd: "wc -c and shasum -a 256 frozen contract"
          status: pass
      harness: codex

  - id: T003
    type: worker
    assignee: Worker
    status: done
    reasoning_hint: medium
    objective: "Implement the focused public WorkflowCanvas vertical slice."
    allowed_files:
      - packages/core/src/workflow-canvas/
      - packages/core/src/index.ts
      - packages/core/src/components/NavMap.tsx
      - packages/demo/app/workflow-canvas/
      - packages/demo/public/workflow-fixtures/
    verify:
      - pnpm test
      - pnpm typecheck
      - pnpm build
    stop_if: []
    receipt:
      result: done
      summary: "Implemented validation, public types and exports, truthful visual and semantic representations, details, evidence, findings, annotations, comparisons, and real fixtures."
      changed_files:
        - packages/core/src/workflow-canvas/
        - packages/core/src/index.ts
        - packages/core/src/components/NavMap.tsx
        - packages/demo/app/workflow-canvas/
        - packages/demo/public/workflow-fixtures/
      commands:
        - cmd: "pnpm exec vitest run packages/core/src/workflow-canvas/validation.test.ts packages/core/src/workflow-canvas/WorkflowCanvas.test.tsx"
          status: pass
      harness: codex

  - id: T004
    type: worker
    assignee: Worker
    status: done
    reasoning_hint: medium
    objective: "Prove responsive, keyboard, screen-reader, reduced-motion, degraded-state, topology, and representative performance behavior."
    allowed_files:
      - packages/core/src/workflow-canvas/
      - scripts/workflow-canvas-browser-proof.mjs
    verify:
      - pnpm test
      - pnpm lint
      - pnpm format:check
      - pnpm typecheck
      - pnpm knip:production
      - pnpm build
      - pnpm --filter @neonwatty/nav-map-scanner exec node ../../scripts/workflow-canvas-browser-proof.mjs
    stop_if: []
    receipt:
      result: done
      summary: "Real Chromium proves exact 393x852 behavior, no overflow, 44x44 targets, focus return, supplied topology, reduced motion, and the 40/60/120 representative fixture within thresholds."
      changed_files:
        - packages/core/src/workflow-canvas/
        - scripts/workflow-canvas-browser-proof.mjs
      commands:
        - cmd: "pnpm test"
          status: pass
        - cmd: "pnpm lint"
          status: pass
        - cmd: "pnpm format:check"
          status: pass
        - cmd: "pnpm typecheck"
          status: pass
        - cmd: "pnpm knip:production"
          status: pass
        - cmd: "pnpm build"
          status: pass
        - cmd: "pnpm --filter @neonwatty/nav-map-scanner exec node ../../scripts/workflow-canvas-browser-proof.mjs"
          status: pass
      harness: codex

  - id: T005
    type: worker
    assignee: Worker
    status: done
    reasoning_hint: medium
    objective: "Finalize public exports, documentation, demo proof, and packed consumer smoke."
    allowed_files:
      - README.md
      - packages/core/src/index.ts
      - scripts/copy-core-css.mjs
      - scripts/package-consumer-smoke.mjs
    verify:
      - pnpm smoke:package-consumer
    stop_if: []
    receipt:
      result: done
      summary: "Documented the focused boundary and proved public ESM, CJS, declarations, stylesheet, scanner, and CLI consumption from packed tarballs."
      changed_files:
        - README.md
        - packages/core/src/index.ts
        - scripts/copy-core-css.mjs
        - scripts/package-consumer-smoke.mjs
      commands:
        - cmd: "pnpm smoke:package-consumer"
          status: pass
      harness: codex

  - id: T006
    type: pm
    assignee: PM
    status: done
    reasoning_hint: high
    objective: "Record the privacy-safe integration-readiness handoff without editing Workflows."
    receipt:
      result: done
      summary: "Recorded workflow-canvas/v1 compatibility, public exports, real fixture proof, and deferred production integration."
      evidence:
        - docs/goals/nav-map-workflow-canvas-embed-mode/notes/T002-supplied-shared-contract.md
      remaining_blockers:
        - "Production React integration and dependency delivery remain deferred to an explicit cross-repository rendezvous."
      harness: codex

  - id: T999
    type: judge
    assignee: Judge
    status: done
    reasoning_hint: high
    objective: "Audit the completed focused WorkflowCanvas owner outcome."
    receipt:
      result: done
      decision: complete
      full_outcome_complete: true
      rationale: "Current full tests, build, packed consumer, exact desktop/mobile Chromium proof, reduced motion, truthful degraded states, semantic topology, and contract hash satisfy the original focused reusable-canvas outcome."
      commands:
        - cmd: "pnpm test"
          status: pass
        - cmd: "pnpm lint"
          status: pass
        - cmd: "pnpm format:check"
          status: pass
        - cmd: "pnpm typecheck"
          status: pass
        - cmd: "pnpm knip:production"
          status: pass
        - cmd: "pnpm build"
          status: pass
        - cmd: "pnpm --filter @neonwatty/nav-map-scanner exec node ../../scripts/workflow-canvas-browser-proof.mjs"
          status: pass
        - cmd: "pnpm smoke:package-consumer"
          status: pass
      harness: codex

  - id: T007
    type: pm
    assignee: PM
    status: active
    reasoning_hint: high
    objective: "Create a long-lived experimental integration branch from main, mirror main's PR protections analogously, commit and push the verified source branch, and open an unmerged PR into the experiment branch."
    inputs:
      - "Current green local verification"
      - "Current GitHub main branch protection and rulesets"
    constraints:
      - "Use only the NavMap repository and remote."
      - "Do not modify or inspect the Workflows repository."
      - "Do not weaken main protections."
      - "Do not merge the PR."
      - "Preserve the dirty unrelated main worktree."
    expected_output:
      - "Protected experimental branch"
      - "Pushed source branch"
      - "Open PR targeting the experimental branch"
    receipt: null

  - id: T008
    type: pm
    assignee: PM
    status: queued
    reasoning_hint: high
    objective: "Audit the experimental branch protection, PR base/head, checks, and preserved local QA evidence."
    receipt: null

checks:
  dirty_fingerprint: "Recovered WorkflowCanvas implementation and control artifacts only on codex/workflow-canvas-embed-mode."
  last_verification:
    result: pass
    task: T999
    commands:
      - pnpm test
      - pnpm lint
      - pnpm format:check
      - pnpm typecheck
      - pnpm knip:production
      - pnpm build
      - pnpm --filter @neonwatty/nav-map-scanner exec node ../../scripts/workflow-canvas-browser-proof.mjs
      - pnpm smoke:package-consumer
